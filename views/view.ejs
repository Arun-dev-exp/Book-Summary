<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Book World</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <div class="container">
        <header class="view-header">
            <nav class="breadcrumb">
                <a href="/" class="breadcrumb-link">← Back to All Books</a>
            </nav>
        </header>

        <main class="full-summary-container">
            <article class="full-summary-card">
                <header class="summary-header">
                    <div class="book-showcase">
                        <div class="book-cover-large">
                            <img 
                                src="https://covers.openlibrary.org/b/olid/<%= post.book_cover_id.trim() %>-L.jpg"
                                alt="Cover for <%= post.title %>"
                                class="cover-image-large"
                                onerror="this.src='/images/default-cover.jpg'"
                            >
                        </div>
                        
                        <div class="book-meta">
                            <h1 class="book-title"><%= post.title %></h1>
                            
                            <div class="book-stats">
                                <div class="rating-display">
                                    <div class="stars">
                                        <% for(let i = 1; i <= 5; i++) { %>
                                            <span class="star <%= i <= Math.floor(post.rating) ? 'filled' : '' %>">★</span>
                                        <% } %>
                                        <% if(post.rating % 1 !== 0) { %>
                                            <span class="star half-filled">★</span>
                                        <% } %>
                                    </div>
                                    <span class="rating-text"><%= post.rating %>/5</span>
                                </div>
                                
                                <div class="summary-date">
                                    <span class="date-label">Reviewed on:</span>
                                    <span class="date-value"><%= new Date(post.summary_date).toLocaleDateString('en-US', { 
                                        year: 'numeric', 
                                        month: 'long', 
                                        day: 'numeric' 
                                    }) %></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </header>

                <section class="summary-content">
                    <h2 class="summary-heading">Book Summary</h2>
                    <div class="summary-text-full">
                        <% 
                        // Split summary into paragraphs for better readability
                        const paragraphs = post.summary.split('\n').filter(p => p.trim());
                        %>
                        <% paragraphs.forEach(paragraph => { %>
                            <p class="summary-paragraph"><%= paragraph.trim() %></p>
                        <% }) %>
                    </div>
                </section>

                <footer class="summary-actions">
                    <a href="/post/<%= post.id %>" class="btn btn-outline action-btn">
                        <span class="btn-icon">✏️</span>
                        Edit Review
                    </a>
                    <a href="/" class="btn btn-primary action-btn">
                        <span class="btn-icon">📚</span>
                        Back to Library
                    </a>
                    <a href="/delete-post/<%= post.id %>" class="btn btn-danger action-btn" onclick="return confirm('Are you sure you want to delete this review?')">
                        <span class="btn-icon">🗑️</span>
                        Delete Review
                    </a>
                </footer>
            </article>
        </main>
    </div>
</body>
</html>